# Nom de l'application
spring.application.name=Chercheur_Service

# Port du serveur
server.port=8081  

# Console H2 (si besoin pour tests)
spring.h2.console.enabled=true

# Configuration de la base de données MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/chercheur-db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Swagger / OpenAPI
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# Matching strategy pour Spring MVC
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# Clé publique pour JWT (si sécurité avec RSA)
rsa.publicKey=classpath:keys/publicKey.pem
